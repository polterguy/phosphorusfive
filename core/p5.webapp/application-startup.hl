

/*
 * Example startup file, that creates the [p5.web.load-ui] Active Event, which is invoked whenever a URL is requested.
 */
create-event:p5.web.load-ui

  /*
   * Creates a widget, simply displaying the URL that was requested, and nothing more.
   */
  create-widget
    parent:cnt
    element:h1
    innerValue:Hello world, URL requested was '{0}'
      :x:/../*/url?value
