
/*
 * Checks to see if the user has access to the given page object
 */

set-protected-event:sys42.verify-access
  set:x:/..?value
    src:bool:true
  if:x:/../*/_arg
    user
    if:x:/-/*/role?value
      not-equals:root
      and:x:/./-/*/role?value
        not-equals:x:/../*/_arg?value
      set:x:/..?value
        src:bool:false


