
/*
 * Startup Hyperlambda that is evaluated when server is started.
 *
 * Evaluates all Hyperlambda files in the "startup" folder, which again are responsible for creating the Navbar,
 * and its associated Active Events.
 */

sys42.utilities.execute-lambda-folder:@BOOTSTRAP/widgets/navbar/startup/
