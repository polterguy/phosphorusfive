
/*
 * Startup Hyperlambda that is evaluated when server is started.
 *
 * Evaluates all Hyperlambda files in the "startup" folder, which again are responsible for creating the most commonly
 * used Bootstrap windows, such as the modal window, etc.
 */

sys42.utilities.execute-lambda-folder:@BOOTSTRAP/windows/startup/
