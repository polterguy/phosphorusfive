
/*
 * Unit tests for [match].
 */


Match yielding a single result
  _data:Thomas Hansen
  match:x:/@_data?value
    src:regex:/Hansen/
  if:x:/@match
    !=:node:@"match
  Hansen"
    throw:Assert error, [match] misbehaved


Match yielding multiple results
  _data:Thomas Hansen er circus hansen
  match:x:/@_data?value
    src:regex:/hansen/i
  if:x:/@match
    !=:node:@"match
  Hansen
  hansen"
    throw:Assert error, [match] misbehaved
