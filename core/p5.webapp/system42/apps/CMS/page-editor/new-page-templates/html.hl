
/*
 * Creates a default [html] type of page, and returns to caller.
 */

/*
 * Returning page to caller.
 */
insert-before:x:
  src
    template:@CMS/page-templates/default.hl
    content:@"<p>Foo, and then a dash of bar too, I guess ...?</p>"

